---
"toml-eslint-parser": major
---

Change to ESM-only package. Now using tsdown for bundling and providing ESM modules only. This is a breaking change that requires Node.js environments that support ESM.
